# C20 Testing System - Environment Configuration

# ======================
# FLASK BACKEND CONFIG
# ======================
FLASK_ENV=production
FLASK_DEBUG=0

# Device Information
DEVICE_NAME=CONNECT 500
DEVICE_TYPE=C20
IP_HOST=192.168.1.10
PORT=8080

# Hardware Mode Configuration
HARDWARE_MODE=mock
# HARDWARE_MODE options: 'mock' or 'serial'
# - mock: Generates simulated sensor data
# - serial: Reads real data from hardware via serial port

# Serial Hardware Configuration (only used when HARDWARE_MODE=serial)
SERIAL_PORT=/dev/ttyUSB0
SERIAL_BAUD=9600
# Expected CSV format from hardware: low,mid,high (pressure values)

# ======================
# PRESSURE SENSOR CONFIG
# ======================
# Mock sensor ranges (used when HARDWARE_MODE=mock)
SENSOR_LOW_MIN=80
SENSOR_LOW_MAX=90
SENSOR_MID_MIN=10
SENSOR_MID_MAX=15
SENSOR_HIGH_MIN=140
SENSOR_HIGH_MAX=160

# ======================
# SECURITY CONFIG
# ======================
# Default admin credentials (change in production)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin

# Session configuration
SECRET_KEY=your-secret-key-change-in-production
SESSION_TIMEOUT=3600

# ======================
# NETWORK CONFIG
# ======================
# Service Ports Configuration
FRONTEND_PORT=80
BACKEND_PORT=5000  
NODEJS_API_PORT=3001

# External Port Mappings (Docker host ports)
FRONTEND_EXTERNAL_PORT=80
BACKEND_EXTERNAL_PORT=5000
NODEJS_API_EXTERNAL_PORT=3001

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost

# API Base URLs  
BACKEND_API_URL=http://localhost:5000
NODEJS_API_URL=http://localhost:3001

# ======================
# DATABASE CONFIG
# ======================
# (Future use - currently using in-memory storage)
# DATABASE_URL=sqlite:///c20test.db

# ======================
# LOGGING CONFIG
# ======================
LOG_LEVEL=INFO
LOG_FILE=/var/log/c20test.log

# ======================
# DOCKER CONFIG
# ======================
# Container timezone
TZ=Europe/Warsaw

# Container user/group IDs (optional)
# PUID=1000
# PGID=1000
